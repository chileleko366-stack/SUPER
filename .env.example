# Copy to .env for local dev. Never commit the real .env (gitignored).
# In GitHub Actions, set these as repo secrets instead:
#   Settings -> Secrets and variables -> Actions -> New repository secret
FIRECRAWL_API_KEY=
NVIDIA_NIM_API_KEY=
